version: '3.8'

services:
  app:
    build: .
    container_name: shop-springboot-app
    ports:
      - "8080:8080"
    environment:
      environment:
        - SPRING_DATASOURCE_URL=jdbc:postgresql://aws-0-ap-southeast-1.pooler.supabase.com:6543/postgres?user=postgres.prhowplexmxxtprjwhgb&password=postgre%40123&sslmode=require
    restart: always