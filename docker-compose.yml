version: '3.8'

services:
  app:
    build: .
    container_name: shop-springboot-app
    ports:
      - "8080:8080"
    environment:
      - DATABASE_URL=jdbc:postgresql://db.prhowplexmxxtprjwhgb.supabase.co:5432/postgres?sslmode=require
      - DATABASE_USERNAME=postgres
      - DATABASE_PASSWORD=postgres@123
    restart: always